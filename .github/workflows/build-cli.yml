name: Build and Commit Motion Core CLI Binaries

on:
  workflow_dispatch:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

defaults:
  run:
    shell: bash

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary: motion-core-cli
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            binary: motion-core-cli
          - os: macos-latest
            target: x86_64-apple-darwin
            binary: motion-core-cli
          - os: macos-14
            target: aarch64-apple-darwin
            binary: motion-core-cli
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary: motion-core-cli.exe
          - os: windows-latest
            target: aarch64-pc-windows-msvc
            binary: motion-core-cli.exe

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux cross linker
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Configure Linux ARM64 toolchain
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc" >> $GITHUB_ENV
          echo "CC_aarch64_unknown_linux_gnu=aarch64-linux-gnu-gcc" >> $GITHUB_ENV
          echo "AR_aarch64_unknown_linux_gnu=aarch64-linux-gnu-gcc-ar" >> $GITHUB_ENV
          echo "CFLAGS_aarch64_unknown_linux_gnu=-march=armv8-a" >> $GITHUB_ENV

      - name: Build CLI binary
        working-directory: motion-core-cli
        run: cargo build --release --package motion-core-cli --target ${{ matrix.target }}

      - name: Stage binary in dist folder
        working-directory: motion-core-cli
        run: |
          mkdir -p js/dist/${{ matrix.target }}
          cp target/${{ matrix.target }}/release/${{ matrix.binary }} js/dist/${{ matrix.target }}/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}
          path: motion-core-cli/js/dist/${{ matrix.target }}/
          if-no-files-found: error

  commit-binaries:
    name: Commit built binaries to master
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: motion-core-cli/js/dist

      - name: List downloaded binaries
        run: ls -R motion-core-cli/js/dist

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and push binaries
        run: |
          git add motion-core-cli/js/dist
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "chore: update motion-core binaries [skip ci]"
          git push origin master
